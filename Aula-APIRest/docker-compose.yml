version: '3'

services:
  postgres:
    image: postgres:15-alpine
    container_name: aula-api-rest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aulavirtual
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
